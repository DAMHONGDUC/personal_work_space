{
  "name": "personal_work_space",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "comment": "The build scripts are .mts run by node directly, which needs the type stripping that became unflagged in 22.18.",
    "node": ">=22.18"
  },
  "scripts": {
    "clean": "rm -rf .next out cv/build public/cv.pdf public/cv",
    "setup": "npm run clean && npm install && npm run cv:tex && node scripts/build-cv-pdf.mts --optional && node scripts/build-cv-images.mts --optional",
    "predev": "npm run setup",
    "dev": "next dev",
    "predev:open": "npm run setup",
    "dev:open": "(until curl -sf -o /dev/null http://localhost:3000; do sleep 0.5; done; open http://localhost:3000) & next dev",
    "build": "next build",
    "preview": "npm run build && npx --yes serve@latest out",
    "lint": "eslint",
    "cv:tex": "node scripts/build-cv-tex.mts",
    "cv:pdf": "npm run cv:tex && node scripts/build-cv-pdf.mts && node scripts/build-cv-images.mts",
    "typecheck": "next typegen && tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "lucide-react": "^1.33.0",
    "next": "16.3.0",
    "radix-ui": "^1.6.7",
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "tailwind-merge": "^3.6.0",
    "tw-animate-css": "^1.4.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@testing-library/dom": "^10.4.1",
    "@testing-library/react": "^16.3.2",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.3.0",
    "jsdom": "^30.0.1",
    "shadcn": "^4.18.0",
    "tailwindcss": "^4",
    "typescript": "^5",
    "vitest": "^4.1.10"
  }
}
